# Phase 1 inheritance benchmark — terminal prelaunch record

Status: **stopped before candidate launch; no Phase 1 result exists**. Benchmark
task `01a01da2-e354-7453-a60b-aa044d111971` is the sole owner of fixtures,
runner, candidate sessions, scoring and independent review. Workbench must not
implement or duplicate them. No benchmark output is a Result, scoped Check,
Decision, Event, Standing change or external validation.

## Reviewed Phase 0 evidence

- preregistration SHA-256:
  `025fb027f948323afda33a846eb9f07dcf78a56f980b6e54fcfbc6728180bbc8`;
- frozen Math packet commit
  `4624ea801c43b773b5d4a8b795c91e1882d8c02b`, tree
  `406a53dffcbb27fbb504987fb2dd8d565026abff`;
- pre-key candidate-output root:
  `sha256:563e5c5d0d785868de169ffea348dbb50d257936075c6bae99c6fe72f2caa2a4`;
- sealed archive SHA-256:
  `5a3404ac0e4108787938a5cb3cab81dc44b9f6c18ad43a85dff717d85a065e45`;
- independent final-review SHA-256:
  `3c6e63712238fab5c078fd04777557d429f66279b2c5d85e68b8cc9e3d0293e3`;
  disposition **Ready for internal sharing**; and
- 18 assigned sessions, 17 valid and one retained 1,371.618-second native
  infrastructure failure with no answer and no retry.

Phase 0 measured Vela at 73/84 answers, raw Git at 65/84 and the native view at
58/84 on three purpose-selected cases. Those values are descriptive internal
evidence only: one trial per arm, one infrastructure failure and one disclosed
judge-family replacement do not support a causal, general, product-lift or
external-validation claim.

## Frozen Phase 1 design

The benchmark owner has frozen 40 roots:

- 20 living Formal Conjectures audit cases at source commit
  `9f5ee773841921f460b4a26a3552f5eca4accaa0`; and
- 20 deterministic KernelBench level-2 paths at official commit
  `423217d9fda91e0c2d67e4a43bf62f96f6d104f1`, tree
  `c4de9ae283314a272a8bd08bec3b246dd8605086`.

Exactly 20 roots exercise a real correction. Each root has four frozen views:
raw history, curated current state, information-matched Git and Vela. The
planned denominator is 160 primary cold-successor sessions plus 40 transfer
sessions. Source-owner roster review, information equality, rights, exact
hardware/evaluator roots and a new preregistration remain benchmark-owner
gates—not Workbench features.

The superseded macOS preregistration bound 200 candidate launches with zero
replacements, 180 seconds per launch, six-way concurrency, 36,000 aggregate
candidate-session seconds and 362 program-wide model calls. The later VM runner
was serial. Direct metered API/provider spend remained USD 0; OAuth token, turn
and tool telemetry was descriptive because that surface did not expose
enforceable ceilings.

## Terminal prelaunch evidence

The final candidate runtime itself passed its deterministic controls:

- candidate VM `01M0F8HWQQHRQCC8DETVCD4QFA`, runtime root
  `sha256:56f72ef619720da0a798aff8ebebfc58afe27e75f0b8d86e7c21348514ddb35d`;
- 160-view boundary-sweep receipt
  `sha256:857ffe81c79b326e3e4b029799ca1c07d916802afc6edc69e24072c407a224f1`;
- exactly one frozen fixture and answer schema were readable while parent auth,
  process state, other fixtures, the host key path and network were denied; and
- forced cancellation and final scrub left no auth, candidate process, zombie,
  fixture, schema or output residue.

Those controls did not authorize a run. The first sealed independent review
failed before all seven target commands because its ambient `mktemp` resolved
against a read-only location. Its terminal report is SHA-256
`8c3698769199bcdbd1375d6a93ce8f9f6c6e4dee12c475d7c34737bffad0cf6a`.

One explicitly authorized successor preregistration repaired only the reviewer
temporary-output scaffold and proved the same private-`/tmp` lifecycle without a
model call. Its exact bindings were:

- preregistration
  `sha256:ba5f8d22c87cd3c730ba3e7d3697e9d1c3533ad591ca690e8df7958cb10bda12`;
- review-bundle root
  `sha256:505567906c32fed8232d87be3624086ab66fe6f27fe3fd4f8b87dbf1111c1d56`;
  and
- zero-model private-`/tmp` lifecycle receipt
  `sha256:17f0ae999e41f1fba9b40244c9308d25119accc4e8fb0585a2f18d8370d48f28`.

The single successor reviewer returned without timeout but never issued the
mandatory `/bin/sh phase1/reviewer_ordinary_probe.sh` command. It produced zero
of seven required observations and narrated an unsupported `mktemp` failure.
A parser field also falsely treated a later `sed` inspection of the script as
an invocation, while the overall gate correctly failed. Exact retained evidence:

- review answer
  `sha256:875633f76b3a25cb6cbbc2bbec664e716f2f26120c8cf0fdce438dd41cb5433a`;
- event stream
  `sha256:3cb86424054f8c0a986725f9f3d6b529a9fd6de7253b5163c24d026973ada295`;
- failure classification
  `sha256:5b3685211d0e1227fb8e19d3977e15cf84ee7d356c312ea226008372940858e7`;
  and
- successor terminal report
  `sha256:f0c2bb4500a5b5add0202a6025722dd0075a7244d36ce6a9fc05901e544064f1`.

The terminal denominator is exact: 40 cases, 160 views and 200 planned
assignments; **zero** candidate sessions, records, retries, launches,
checkpoints, scores or launch-authorization records. The held-out key remained
absent from candidate-visible bytes.

## No-go boundary

Phase 1 must not resume by weakening isolation, silently retrying the reviewer,
or asking another model to attest to controls it did not execute. Any later
successor requires a new explicit authorization and a deterministic host-owned
prelaunch gate proving all of the following without relying on reviewer
narrative:

- the candidate sees exactly one frozen fixture and its answer schema, never a
  held-out key, another fixture, host home or benchmark control file;
- authentication is available to the Codex parent but absent from candidate
  files, inherited environment and readable process state before model tool
  execution;
- candidate shell/network policy is active and fail closed;
- numeric launch, wall, retry, failure and aggregate cost policies are bound to
  the current preregistration, with token, turn and tool telemetry explicitly
  descriptive rather than falsely presented as an enforceable cap;
- cancellation leaves no surviving candidate process; and
- the disposable runtime and exact inputs can be reconstructed without a
  provider or host-home dependency.

Failed macOS Seatbelt, Docker/lower-layer, nested-sandbox and reviewer-tool
attempts are control evidence only. They do not authorize a weaker boundary and
are not candidate records.

## Decision use

Phase 0 remains the only scored inheritance evidence and is descriptive and
internal. Phase 1 supplies a no-go about this runner's prelaunch reviewer, not a
finding about Vela, Git, Formal Conjectures, KernelBench or any scientific
candidate. Do not reproduce scoring or tune Vela from these failed controls.
Nothing here unfreezes Core or Workbench: new machinery still requires the same
concrete blocking failure from at least two real maintained Result consumers.
